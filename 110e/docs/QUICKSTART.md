# 🚀 前端快速启动指南

本文档将帮助你快速搭建和启动酒店中央温控系统前端项目。

---

## 📋 环境要求

| 软件 | 版本 | 说明 |
|------|------|------|
| Node.js | 18+ | JavaScript 运行环境 |
| npm | 9+ | 包管理工具 |
| IDE | VS Code 或 Cursor | 推荐使用 |

---

## ✅ 环境检查

### 检查 Node.js 和 npm

```bash
# 检查 Node.js 版本
node -v
# 应显示 v18.x.x 或更高

# 检查 npm 版本
npm -v
# 应显示 9.x.x 或更高
```

### 安装 Node.js (如未安装)

**macOS:**
```bash
brew install node@18
```

**Windows/Linux:**
下载地址：https://nodejs.org/

---

## 🔧 项目安装

### 第一步：克隆项目

```bash
git clone <repository-url>
cd central-temperature-control-system/front-end
```

### 第二步：安装依赖

```bash
npm install
```

**注意**：首次安装可能需要几分钟，请耐心等待。

**如果安装失败**，可以尝试使用国内镜像：

```bash
# 配置淘宝镜像
npm config set registry https://registry.npmmirror.com

# 重新安装
npm install
```

---

## 🖥️ 启动开发服务器

### 确保后端已启动

前端需要连接后端 API，请先确保后端服务已启动：

```bash
# 在 back-end 目录下启动后端
cd ../back-end
mvn spring-boot:run
```

后端默认运行在：`http://localhost:8080`

### 启动前端

```bash
# 在 front-end 目录下
npm run dev
```

看到以下输出表示启动成功：

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

---

## 🌐 访问系统

在浏览器中打开：**http://localhost:5173**

你将看到系统首页，包含四个角色选择：
- 🏠 **首页** - 系统介绍
- 💼 **前台** - 办理入住/退房
- 👤 **客户** - 房间控制
- 👨‍💼 **管理员** - 系统监控
- 📊 **经理** - 数据统计

---

## ✅ 功能测试

### 1. 前台办理入住

```
路径: 前台 → 办理入住
步骤:
1️⃣ 选择房间 (如: 101)
2️⃣ 填写客户信息
3️⃣ 设置空调模式
4️⃣ 确认支付押金
```

### 2. 客户使用空调

```
路径: 客户 → 选择房间
功能:
- 开机/关机
- 调节温度 (18-30°C)
- 调节风速 (低/中/高)
- 切换模式 (制冷/制热)
- 查看实时费用
```

### 3. 管理员监控

```
路径: 管理员 → 系统监控
功能:
- 查看所有房间状态
- 查看服务队列
- 查看等待队列
- 强制开关机
```

### 4. 经理统计

```
路径: 经理 → 数据统计
功能:
- 选择时间范围
- 生成统计报表
- 查看图表分析
```

---

## 🔧 配置说明

### API 配置

配置文件：`src/config/index.ts`

```typescript
// API 模式: 'mock' | 'api'
export const API_MODE: 'mock' | 'api' = 'api';

// API 基础路径
export const API_BASE_URL = '/api';
```

**模式说明**：
- `api` - 使用真实后端 API（默认）
- `mock` - 使用模拟数据（开发测试用）

### Vite 配置

配置文件：`vite.config.ts`

```typescript
export default defineConfig({
  server: {
    port: 5173,      // 前端端口
    open: true,      // 自动打开浏览器
    proxy: {
      '/api': {
        target: 'http://localhost:8080',  // 后端地址
        changeOrigin: true,
      },
    },
  },
})
```

---

## 🐛 常见问题

### 1. 端口被占用

**错误信息**：
```
Port 5173 is already in use
```

**解决方案**：

方法1：修改端口
```typescript
// vite.config.ts
export default defineConfig({
  server: {
    port: 5174,  // 改为其他端口
  }
})
```

方法2：关闭占用端口的进程
```bash
# macOS/Linux
lsof -ti:5173 | xargs kill -9

# Windows
netstat -ano | findstr :5173
taskkill /PID <进程ID> /F
```

### 2. 依赖安装失败

**错误信息**：
```
npm ERR! code ERESOLVE
```

**解决方案**：

```bash
# 清理缓存
npm cache clean --force

# 删除依赖
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 3. 接口调用失败

**错误信息**：
```
Failed to fetch
Network Error
```

**检查清单**：

1. 后端是否已启动？
   ```bash
   curl http://localhost:8080/api/room/states
   ```

2. Vite 代理配置是否正确？
   ```typescript
   // vite.config.ts - 检查 proxy 配置
   proxy: {
     '/api': {
       target: 'http://localhost:8080',
       changeOrigin: true,
     },
   }
   ```

3. 浏览器控制台是否有 CORS 错误？
   - 后端应已配置 CORS，如仍有问题请检查后端 `WebConfig.java`

### 4. TypeScript 类型错误

**解决方案**：

```bash
# 重新生成类型定义
npm run type-check

# 如果使用 VSCode，重启 TS Server
# 命令面板: TypeScript: Restart TS Server
```

---

## 📦 生产部署

### 构建生产版本

```bash
# 构建
npm run build

# 预览构建结果
npm run preview
```

构建完成后，`dist/` 目录包含所有静态文件。

### 部署到服务器

**使用 Nginx：**

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 前端静态文件
    location / {
        root /path/to/dist;
        try_files $uri $uri/ /index.html;
    }
    
    # 代理后端 API
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 📖 下一步

- 📚 阅读 [项目结构文档](PROJECT_STRUCTURE.md) 了解代码组织
- 📚 阅读 [入住流程文档](CHECKIN_PROCESS.md) 了解业务流程
- 📚 阅读 [API 使用指南](API_GUIDE.md) 了解接口对接
- 🔧 查看 [后端文档](../../back-end/docs) 了解API详情

---

## 📞 获取帮助

如果遇到问题：

1. 查看本文档的"常见问题"部分
2. 检查浏览器控制台的错误信息
3. 查看前端开发者工具的 Network 面板
4. 参考项目文档和源代码注释

---

**祝你使用愉快！** 🎉
